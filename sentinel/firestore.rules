rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow all access for demo/testing (emulator mode)
    // In production, replace with proper authentication rules
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
