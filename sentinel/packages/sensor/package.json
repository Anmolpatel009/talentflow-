{
  "name": "@sentinel/sensor",
  "version": "0.1.0",
  "description": "Telemetry collection middleware for Sentinel-Node",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@sentinel/core": "*",
    "firebase": "^12.10.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  },
  "peerDependencies": {
    "next": "^16.1.6"
  },
  "publishConfig": {
    "access": "restricted"
  }
}
